#include <stdio.h>
#include <string.h>

void REVERSE(char s[])
{
    int i,j;
    char t;
    int n=strlen(s);
    for(i=0,j=n-1;i<j;i++,j--){
        t=s[i];s[i]=s[j];s[j]=t;
    }
}

int main()
{
    char s[1000];
    scanf("%[^\n]",s);
    REVERSE(s);
    printf("%s",s);
}
